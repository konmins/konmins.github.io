# 仓库内统一使用 LF。
# 这个项目是「Windows 上开发 + Linux 上构建」，不锁定换行符的话，
# 每次在 Windows 上编辑都会把整个文件标记成改动，diff 完全没法看。
* text=auto eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.avif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
